# Daily Price 项目全局开发规范

## 1. 依赖管理（严格锁定）
- 绝对禁止修改 `pubspec.yaml` 中的依赖版本，除非我明确要求。
- `csv` 插件版本必须锁定为 `5.0.2`。在使用相关方法时，必须保留 `const ListToCsvConverter()` 和 `const CsvToListConverter()`。
- 不允许随意引入新插件，如果必须引入，请先在对话中向我确认。

## 2. 核心架构约束
- **UI 布局**：不要使用 Drawer 侧边栏。导航栏左侧为“筛选”和“刷新”，右侧为“排序”、“添加”、“设置”。
- **状态管理**：凡是涉及用户习惯的设置（如默认视图、排序偏好），必须使用 `shared_preferences` 进行本地持久化。
- **数据库交互**：导入数据时，必须使用 `upsert` 并指定 `onConflict: 'id'` 以防止重复数据。

## 3. 语法与空安全
- 严格遵守 Dart 的 null safety 规则。在 map 构造或复杂条件判断中，如果已确认 nullable 变量不为空，请使用 `!` 断言（例如 `soldDate!.toIso8601String()`），避免类型推断失败。
- 对于 `String?` 类型的判断，必须先写 `!= null` 再写 `.isNotEmpty`。

## 4. 工具使用规范
- 在执行重构任务时，每次尽量只使用工具修改 1 到 2 个文件，避免单次输出过长导致 XML 标签截断或 JSON 解析错误。